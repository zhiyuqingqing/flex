server:
  port: 8889


# mybatis-plus
mybatis-plus:
  global-config:
    db-config:
      id-type: auto
  mapper-locations: classpath:mapperxml/*.xml

  #configuration:
  #log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    db1:
      jdbc-url: jdbc:mysql://localhost:3306/eladmin
      username: root
      password: root

    db2:
      jdbc-url: jdbc:mysql://localhost:3306/pig
      username: root
      password: root
  redis:
    host: localhost
    port: 6379
    password:



